language: julia

notifications:
  email: false

jobs:
  include:
    script:
      julia: 1.5
      os: linux
        - julia --color=yes Test.jl -e 'using Pkg; Pkg.activate(); Pkg.instantiate(); Pkg.test()'
      after_success: skip
